void main() {
  // // 创建固定长度是 5 的 list, 默认填充 0
  // List<int> list = List.filled(5, 0);
  // list[0] = 5;
  // print(list);
  // print(list.isEmpty);
  // print(list.isNotEmpty);
  // List listReversed = list.reversed.toList(); // reverse list
  // print(listReversed);

  // List list = [1, 2, 3];
  // list.add(4); // 给 list 追加一个 item
  // list.addAll([5, 6]); // 给 list 追加多个 item
  // print(list);
  // print(list.indexOf(1)); // 返回指定 value 的 index, 找不到返回 -1
  // list.remove(1); // 根据 value 删除 item, 如果有多个, 只会删除一个
  // print(list);
  // list.removeAt(0); // 删除指定 index 的 item
  // print(list);
  // list.removeRange(0, 2); // 删除指定 index 范围内的 item
  // print(list);
  // list.fillRange(0, 2, 5); // 将指定 index 范围内的 item 替换成 5
  // print(list);
  // list.insert(1, 3); // 指定 index insert 一个 item
  // print(list);
  // list.insertAll(1, [1, 2]); // 指定 index insert 多个 item
  // print(list);
  // String listStr = list.join(',');
  // print(listStr);
}
